apiVersion: v1
kind: Service

metadata:
  name: alert-service
  namespace: hackathon

spec:
  selector:
    app: alert-service

  ports:
  - protocol: TCP
    port: 8091
    targetPort: 8091

  type: ClusterIP
